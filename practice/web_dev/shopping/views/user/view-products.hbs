<section>
    <div class="container mt-5">
        <div class="row">
            {{#each product}}

            <div class="col-md-3 p-3">
                <div class="card " style="width: 15rem; ">
                    <img src="/product-images/{{this._id}}.png" alt="..." style="height: 15rem;" />
                    <div class="card-body">
                        <h5 class="card-title">{{this.name}}</h5>
                        <p class="card-text">{{this.description}}</p>
                        <p>Price ₹{{this.price}}</p>
                        <div>
                        <button onclick="addToCart('{{this._id}}')" class="btn btn-primary ml-5">Add to Cart</button>
                        </div>
                    </div>
                </div>
            </div>
            {{/each}} 
        </div>
    </div>
</section>

